# Docs

Documentation for the independent waveform-line dataset maker.

- `method.md`: describes the synthetic DAS waveform model, vehicle-line
  geometry, rendering coordinate system, and binary label construction.
- `model_method.md`: describes the standalone segmentation model, loss, and
  skeleton postprocess used for waveform-line prediction.
